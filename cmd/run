#!/bin/sh
CMD=CMD/r
BUILD=;
CDN=;
ENV=dev
WEB=cmd/r/server
WKR=;


source cmd/b/init

# if [ "$2" = "--worker" ]; then
#   L2 "r.worker: still TODO"
#   WKR=cmd/r/worker
# fi

# if [ "$1" = "--dist" ]; then
#   L1 "dist.env TODO"
#   ENV=prod
#   BUILD=cmd/b/dist
# elif [ "$1" = "--test" ]; then
#   L1 "test.env"
#   CDN=;
# elif [ "$1" = "--dev" ]; then
  # L1 "dev.env"
  # BUILD=;
# else
  # BUILD=cmd/  imm m mm /dev
  # CDN=cmd/r/cdn
  WEB=cmd/r/watch
# fi


if [ "$BUILD" != "" ]; then source $BUILD; fi
if [ "$CDN" != "" ]; then source $CDN; fi
if [ "$WKR" != "" ]; then source $WKR --env $ENV & fi
if [ "$WEB" != "" ]; then source $WEB --env $ENV; fi
